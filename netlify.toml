[build]
  command = "npm run build"
  publish = "frontend/dist"

[build.environment]
  NODE_VERSION = "20"

# Single-page application redirects for React
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
